services:
    postgresql:
        image: postgres:latest
        restart: always
        ports:
          - "5432:5432"
        environment:
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: integrated-test-pwd
          POSTGRES_DB: db-spw
        volumes:
          - postgres_data:/var/lib/postgresql/data